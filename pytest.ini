# pytest.ini
[pytest]
pythonpath = .
testpaths = test
addopts = --strict-config --strict-markers
markers =
    tier1: Fast unit and boundary tests (< 15s)
    tier2: Robustness, property-based fuzzing and fault injection (< 2m)
    tier3: Chaos engineering and deep golden master replays



